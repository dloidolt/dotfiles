- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.gitignore:
    ~/.gvimrc:
    ~/.vim:
    ~/.vimrc:
    ~/.zshenv:
    ~/.zlogin:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
