- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.gitignore:
    ~/.gvimrc:
    ~/.p10k.zsh:
    ~/.vim:
    ~/.vimrc:
    ~/.zshenv:
    ~/.zlogin:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
