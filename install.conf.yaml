- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.gitignore:
    ~/.gvimrc:
    ~/.vim:
    ~/.vimrc:
    ~/.zlogin:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
